name: CodeQL

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  schedule:
    # Weekly, Monday 04:17 UTC.
    - cron: "17 4 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Minor version only: setup-go with check-latest resolves it to the newest
  # 1.27.x patch release, so CI always tests with the current toolchain.
  GO_VERSION: "1.27"

jobs:
  analyze:
    name: Analyze (Go)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      # Upload of the SARIF results.
      security-events: write
      # The action reads its own workflow run (status report, diff range)
      # and fails with "Resource not accessible by integration" without it.
      actions: read
      # Download of the CodeQL query packs from the GitHub package registry.
      packages: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ env.GO_VERSION }}
          check-latest: true
          cache: true

      # The CodeQL actions are referenced by their v3 tag on purpose: Dependabot
      # (github-actions ecosystem, see .github/dependabot.yml) pins them to a
      # commit SHA with its first update and keeps the pin current afterwards.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: go
          build-mode: manual
          queries: security-and-quality

      # Manual build so all three modules are traced, not only the root one.
      - name: Build all modules
        run: |
          go build ./...
          for module in services/ai-logs-receiver services/synit-llm-guard; do
            (cd "$module" && go build ./...)
          done

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:go"
